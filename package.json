{
  "name": "eslint-plugin-test-no-only",
  "version": "1.0.0",
  "description": "eslint rules to avoid test.only",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mappy",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^5.14.0"
  }
}
